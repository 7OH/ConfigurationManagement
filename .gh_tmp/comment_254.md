Исправлено в **0.3.9.0** (Windows/WPF).

Правка 0.3.8.26 задавала `VerticalContentAlignment=Center` ячейкам `DataGrid`, но текст текстовых колонок рендерится через сгенерированный `TextBlock`, растянутый на всю высоту ячейки, поэтому стоял выше галочки/индикатора/кнопки. Добавлен `ElementStyle` с `VerticalAlignment=Center` для всех текстовых колонок — элементы каждой строки теперь выровнены по одной линии.

Обе сборки (Windows/WPF и Linux/Avalonia) проходят без ошибок. Issue не закрываю.